import { StyleSheet } from 'react-native';
import {
  secondaryColor,
} from '../../styles/common';

export default StyleSheet.create({
  background: {
    flex: 1,
    backgroundColor: secondaryColor,
  },
});
